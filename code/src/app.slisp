(include "./src/macros")

(ed/use-port 'main-port
  ('get-widgets ('ok (widgets) 
                  (.log console widgets))
                ('error (exc) 
                  (.log console exc))))

; timeout!
; (ed/send-port 'main-port 'get-widgets (nil))